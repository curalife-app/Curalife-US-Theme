{% if shop.name == "CuraLife" %}
  {% assign curaeats_value = 29 %}
  {% assign challenge_value = 99 %}
{% elsif shop.name == "CuraLife Global" %}
  {% assign curaeats_value = 24 %}
{% endif %}

<div class="catalog-page">
  <div class="container">
    {% render 'collection-titles' %}
    {% render 'products-catalog-section', Prods: collection.products, challenge_value: challenge_value, curaeats_value: curaeats_value, isLinkToProduct: true %}
    {% render 'trust-badges-section', colors: "#5c5c5c" %}
  </div>

  {% render 'bundle-gifts-section', challenge_value: challenge_value, curaeats_value: curaeats_value %}
</div>