<style>
  @font-face {
    font-family: 'DM Sans';
    font-style: normal;
    font-weight: 400;
    src: url('{{ "DMSans-Regular.ttf" | asset_url }}');
    src: local(''), url('{{ "DMSans-Regular.ttf" | asset_url }}') format('ttf'),
  }

  @font-face {
    font-family: 'DM Sans';
    font-style: normal;
    font-weight: 500;
    src: url('{{ "DMSans-Medium.ttf" | asset_url }}');
    src: local(''), url('{{ "DMSans-Medium.ttf" | asset_url }}') format('ttf'),
  }

  @font-face {
    font-family: 'DM Sans';
    font-style: normal;
    font-weight: 700;
    src: url('{{ "DMSans-Bold.ttf" | asset_url }}');
    src: local(''), url('{{ "DMSans-Bold.ttf" | asset_url }}') format('ttf'),
  }

  @font-face {
    font-family: 'PP Radio Grotesk';
    font-style: normal;
    font-weight: 300;
    src: url('{{ "PPRadioGrotesk-Light.otf" | asset_url }}');
    src: local(''), url('{{ "PPRadioGrotesk-Light.otf" | asset_url }}') format('otf'), }

  @font-face {
    font-family: 'PP Radio Grotesk';
    font-style: normal;
    font-weight: 400;
    src: url('{{ "PPRadioGrotesk-Regular.otf" | asset_url }}');
    src: local(''), url('{{ "PPRadioGrotesk-Regular.otf" | asset_url }}') format('otf'), }

  @font-face {
    font-family: 'PP Radio Grotesk';
    font-style: normal;
    font-weight: 700;
    src: url('{{ "PPRadioGrotesk-Bold.otf" | asset_url }}');
    src: local(''), url('{{ "PPRadioGrotesk-Bold.otf" | asset_url }}') format('otf'), }
</style>

<section id="main-title-section">
  <div class="container">
    <div class="onboarding-thank-you">
    <h1>Thank You!</h1>
    <div class="green-small-border"></div>
    <p>Keep going, one step at a time. <br/>
    We are here with you!</p>
  </div>
</section>

<style>
  #main-title-section .container {
    text-align: center;
  }

  #main-title-section .container .onboarding-thank-you {
    font-family: 'PP Radio Grotesk';
    font-weight: 400;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    color: #424733;
  }

  #main-title-section .container .onboarding-thank-you  h1 {
    font-size: 60px;
    line-height: 72px;
  }

  #main-title-section .container .onboarding-thank-you .green-small-border {
    width: 40%;
    width: 30%;
    border: 2px solid #424733;
  }

  #main-title-section .container .onboarding-thank-you p {
    font-size: 32px;
    line-height: 50px;
    color: #424733;
  }

  @media (max-width: 768px) {
    #main-title-section .container .onboarding-thank-you  h1 {
      font-size: 32px;
    }

    #main-title-section .container .onboarding-thank-you .green-small-border {
        width: 17vw;
    }

    #main-title-section .container .onboarding-thank-you p {
      font-size: 18px;
      line-height: 30px;
    }
  }
</style>

<script>
  const urlParams = new URLSearchParams(window.location.search);

  if (urlParams.get('customer') && urlParams.get('field') && urlParams.get('value')) {
    // Split to flow by Email or ID
    let zapierURL = urlParams.get('customer').includes('@') ? 'https://hooks.zapier.com/hooks/catch/10900898/bxyk2z1/' : 'https://hooks.zapier.com/hooks/catch/10900898/bxnq9au/';

    const options = {
      method: 'POST',
      body: JSON.stringify({customer: urlParams.get("customer"), field: urlParams.get("field"), value: urlParams.get("value")})
    };

    fetch(zapierURL, options)
    .then(response => console.log(response.json()))
    .then(response => console.log(response))
    .catch(err => console.error(err));
  }
  else {
    console.log("No Params");
    $("h1").text("Sorry");
    $("p").htme("There was an error with the page,<br> please contact us for support");
  }
</script>