{% assign experienses = "“Curalife has kept my sugar within normal ranges even after I eat things that I probably shouldn't!”*Alon M.
|“This is the greatest! My husband started taking this! Finally found a product that works! “*Linda V.
|“I have tried so many different supplements with no results. This one is amazing and i am excited. I just purchased my second bottle.”*Michael Murillo Sr.
|“It is so wonderful to find something that really works. I feel so much better!!”*Leanne A.
|“Have been taking CuraLin over the last four months and am beyond pleased with the results.”*Michael E.

" | split: '|' %}

<section class="pack-info-section instance-{{ index }}">
  <div class="container">
   
    <div class="tabbed-content tabs-side">
      <nav class="tabs">
        <ul>
          <li><a href="#health-benefits-{{ index }}" class="active">Benefits/Description <i>></i></a></li>
          <li><a href="#instructions-{{ index }}">Usage Instructions <i>></i></a></li>
          {% if product.metafields.custom.ingredients.value %}
            <li><a href="#ingredients-{{ index }}">Ingredients <i>></i></a></li>
          {% endif %}
          <li><a href="#facts-{{ index }}">Reviews <i>></i></a></li>
        </ul>
      </nav>

      <div class="content">
        <div id="health-benefits-{{ index }}" class="item active always-active" data-title="Benefits/Description">
          <div class="health-benefits item-content">
          {% for benefit in product.metafields.custom.benefits_images.value %}
            <div class="health-benefit">
             
              <img src="{{ benefit.src | image_url }}" alt="health benefit" loading="lazy">
              <p>{{ benefit.alt }}</p></div>
           
          {% endfor %}
          </div>
        </div>

        <div id="instructions-{{ index }}" class="item" data-title="Usage Instructions">
          <div class="instructions item-content">
            {% for step in product.metafields.custom.instructions.value %}
              <div class="step">
                <h3>{{ step.title }}</h3>

                <div class="capsules">
                  {% for item in (1..step.capsules) %}
                    {% if product.tags contains "gummies" %}
                      <img class="gummies" src="{{ "onboarding-gummy.png" | asset_url }}" width="20" height="31" alt="Capsule Pill" loading="lazy">
                    {% else %}
                      <img class="caps" src="{{ "onboarding-capsule.png" | asset_url }}" width="20" height="31" alt="Capsule Pill" loading="lazy" style="min-width: 35px">
                    {% endif %}
                  {% endfor %}
                </div>
                <p>{{ step.subtitle }} <br> {{ step.text }} <br> {{ step.text_2 }}</p>

              </div>
            {% endfor %}
          </div>
        </div>

        {% if product.metafields.custom.ingredients.value %}
          <div id="ingredients-{{ index }}" class="item" data-title="Ingredients">
            <div class="ingredients item-content">
              {% for ingredient in product.metafields.custom.ingredients.value %}
                <div class="ingredient">
                  {% assign ingredient_handle = ingredient | downcase | replace: ' ', '-' %}
                  <img src="{{ "product-supplement-pack-info-ingredient-icon-" | append: ingredient_handle | append: ".png" | asset_url }}" alt="Ingredient" loading="lazy">
                  <p>{{ ingredient }}</p>
                </div>
              {% endfor %}
            </div>
          </div>
        {% endif %}

        <div id="facts-{{ index }}" class="item" data-title="Reviews">
          <div class="facts item-content">
              {% render 'curalin-pack-changing-experience', experienses: experienses, onlyData: true %}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

{% if isCuralinRender %}
  <style>
   .content {
     max-width: 100%;
    }
    #supplement-page section.pack-info-section .titles {
      flex-direction: revert;
      justify-content: start;
      max-width: 100%;
      text-align: left;
    }

    .pack-info-section {
     
    }

    .pack-info-section:nth-of-type(even) {
      background: {{ bg_color }};
    }

    .pack-info-section .product-image-wrapper {
      position: absolute;
      bottom: -120px;
      right: 0;
      pointer-events: none;
    }

    .pack-info-section:nth-of-type(even) .tabbed-content {
      margin-left: 30%;
    }

    .pack-info-section:nth-of-type(even) .product-image-wrapper,
    .pack-info-section:nth-of-type(even) .product-image-wrapper p{
      left: 0;
    }

    .pack-info-section .product-image-wrapper .number {
      font-family: var(--main-font);
      position: absolute;
      right: 0;
      top: -80px;
      line-height: 1em;
      font-size: 350px;
      font-weight: bold;
      color: var(--dark-orange);
      margin: unset;
    }

    .pack-info-section.pack-info-section:nth-of-type(even) .product-image-wrapper .number {
      color: {{ number_color }};
    }

    .pack-info-section .product-image-wrapper .product-image {
      position: relative;
      right: -10px;
    }

    .pack-info-section .health-benefits {
      display: flex;
      flex-wrap: wrap;
      gap: 30px;
    }

    .pack-info-section .health-benefits .health-benefit {
      display: flex;
      align-items: center;
      width: 40%;
      margin: auto;
      gap: 20px;
    }




    .pack-info-section .health-benefit-icon {
      background: var(--light-green);
      color: white;
      border-radius: 50%;
      min-width: 30px;
      height: 30px;
      font-size: 30px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-right: 20px;
    }

    .pack-info-section .instructions {
      display: flex;
      flex-direction: column;
      gap: 30px;
    }

    .pack-info-section .instructions .step {
      display: flex;
      flex-wrap: wrap;
      gap: 30px;
    }

    .pack-info-section .instructions .step h3 {
      font-size: 28px;
      font-weight: bold;
      min-width: 105px;
    }

    .pack-info-section .instructions .step .capsules {
      min-width: 80px;
    }

    .pack-info-section .ingredients {
      display: flex;
      flex-wrap: wrap;
      max-width: 400px;
      gap: 10px;
    }

    .pack-info-section .ingredient {
      width: 120px;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .pack-info-section .ingredient p {
      font-size: 16px;
      text-align: center;
    }

    .pack-info-section .facts {
      text-align: center;
    }

    .pack-info-section .facts img {
      max-height: 300px;
    }

    .pack-info-section .tabbed-content {
      max-width: 800px;
      display: flex;
      align-items: center;
    }

    .pack-info-section .tabs ul li {
      text-align: center;
      height: 70px;
      border-bottom: 1px solid;
    }

    .pack-info-section .tabs ul li:first-child {
      border-top: 1px solid;
    }

    .pack-info-section .tabs ul li a {
      height: 100%;
      align-items: center;
      display: flex;
      justify-content: center;
    }

    .pack-info-section .tabs ul li a i {
      position: relative;
      transition: all 0.5s ease-in-out;
      font-style: normal;
      left: 20px;
    }

    .pack-info-section .tabs ul li a:hover,
    .pack-info-section .tabs ul li a.active {
      color: var(--brown);
      font-weight: bold;
    }

    .pack-info-section:nth-of-type(even) .tabs ul li a:hover,
    .pack-info-section:nth-of-type(even) .tabs ul li a.active {
      color: {{ link_color }};
    }

    .pack-info-section .item {
      margin-bottom: 2px;
      position: relative;
    }

    .pack-info-section .item::before {
      content: attr(data-title);
      border-top: 1px solid var(--light-green);
      cursor: pointer;
      font-weight: bold;
      padding: 0.5em;
      display: block;
      text-align: center;
    }

    .pack-info-section .item.active .item-content {
      transition: opacity 0.8s ease-in-out;
    }

    .pack-info-section .tabbed-content .tabs {
      display: none;
    }

    .tabbed-content .item {
      min-height: 2em;
    }

    .pack-info-section .tabbed-content .item .item-content {
      opacity: 0;
      visibility: hidden;
      height: 0;
    }

    .pack-info-section .tabbed-content .item.active .item-content {
      opacity: 1;
      visibility: visible;
      height: auto;
    }

    @media all and (min-width: 1130px) {

    .pack-info-section .tabbed-content {
      max-width: unset;
    }

      .pack-info-section .health-benefits {
          justify-content: center;
        }
      .item.active .item-content {
        padding-top: 0;
      }

      .pack-info-section .tabbed-content .tabs {
        display: block;
      }

      .pack-info-section .tabbed-content .tabs li {
        display: inline-block;
        margin-bottom: 2px;
      }

      .pack-info-section .tabbed-content .tabs li a {
        display: flex;
      }

      .pack-info-section .tabbed-content .item {
        min-height: 0;
      }

      .pack-info-section .tabbed-content .item::before {
        display: none;
      }

      .pack-info-section .tabbed-content.tabs-side .tabs {
        min-width: 300px;
        margin-right: 50px;
      }

      .pack-info-section .tabbed-content.tabs-side .tabs li {
        display: block;
      }
    }

    @media all and (max-width: 1130px) {
      .pack-info-section {
        padding-bottom: 75vw;
      }

      .pack-info-section .tabbed-content {
        display: block;
        margin-left: unset !important;
      }

      .pack-info-section .item::after {
        content: "+";
        font-size: 30px;
        position: absolute;
        top: 0.2vw;
        right: 2vw;
        border: 2px solid var(--light-green);
        border-radius: 50%;
        width: 5vw;
        height: 5vw;
        font-size: 6vw;
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--light-green);
      }

      .pack-info-section .item.active::after {
        content: "-";
      }

      .pack-info-section .product-image-wrapper {
        bottom: 0;
        width: 100%;
        position: relative;
        text-align: center;
        margin-top: 10vw;
      }

      .pack-info-section .product-image-wrapper .product-image {
        right: unset;
        text-align: center;
      }

      .pack-info-section .product-image-wrapper .number {
        top: -15vw;
        font-size: 70vw !important;
        right: 13vw;
      }

      .pack-info-section .tabbed-content .item.active .item-content {
        padding: 5vw 0;
      }

      .pack-info-section .ingredient {
        width: 27vw;
      }

      .pack-info-section .instructions .step {
        gap: 2vw;
      }

      .pack-info-section .instructions .step h3 {
        min-width: 20vw;
      }
    }

    @media screen and (max-width: 500px) {
      .pack-info-section .health-benefits .health-benefit {
        gap: 2vw;
      }
      .pack-info-section .health-benefits .health-benefit p {
        font-size: 2.5vw;
      }
    }
  </style>
{% endif %}

  <script>
    tabControl();

    var resizeTimer;
    $(window).on('resize', function(e) {
      clearTimeout(resizeTimer);
      resizeTimer = setTimeout(function() {
        tabControl();
      }, 250);
    });

    function tabControl() {
      var tabs = $('.pack-info-section.instance-{{ index }} .tabbed-content').find('.tabs');
      if(tabs.is(':visible')) {
        tabs.find('a').on('click', function(event) {
          event.preventDefault();
          var target = $(this).attr('href'),
              tabs = $(this).parents('.tabs'),
              buttons = tabs.find('a'),
              item = tabs.parents('.pack-info-section.instance-{{ index }} .tabbed-content').find('.item');
          buttons.removeClass('active');
          item.removeClass('active');
          $(this).addClass('active');
          $(target).addClass('active');
        });
      } else {
        $('.item').on('click', function() {
          var container = $(this).parents('.tabbed-content'),
              currId = $(this).attr('id'),
              items = container.find('.item');
              console.log(items);
          container.find('.tabs a').removeClass('active');
          items.removeClass('active');
          $(this).addClass('active');
          container.find('.tabs a[href$="#'+ currId +'"]').addClass('active');
        });
      }
    }
  </script>
