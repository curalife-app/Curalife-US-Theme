{% if shop.name == "CuraLife" %}
  {% assign shipping_worldwide = 'US Free Shipping' %}
{% elsif shop.name == "CuraLife Global" %}
  {% assign shipping_worldwide = 'Free Shipping Above £99' %}
{% endif %}

<section id="trust-badges-section">
  <div class="container">
    <div class="trust-badge"><img src="{{ 'icon-free-shipping.png' | asset_url }}" alt="" loading="lazy">{{ shipping_worldwide }}</div>
    <div class="trust-badge"><img src="{{ 'icon-money-back.png' | asset_url }}" alt="" loading="lazy">60 Day Money Back Guarantee</div>
    <div class="trust-badge"><i class="icofont-ui-lock"></i>100% Secure Checkout</div>
  </div>
</section>

<style>
  #trust-badges-section {
      padding: 30px 0;
  }

  #trust-badges-section .container {
      display: flex;
      justify-content: center;
      gap: 50px;
      font-size: 18px;
  }

  #trust-badges-section .trust-badge {
      display: flex;
      flex-direction: row;
      gap: 10px;
      text-align: center;
      align-items: center;
      text-transform: uppercase;
  }

  #trust-badges-section .trust-badge img {
      margin-right: 10px;
      height: 5vw;
      width: 10vw;
      object-fit: contain;
      max-width: 40px;
      max-height: 30px;
  }

  @media screen and (max-width:760px) {
    #trust-badges-section {
        gap: unset;
        justify-content: space-between;
sP    }

    #trust-badges-section .trust-badge {
        font-size: 3vw;
        flex-direction: column;
    }

    #trust-badges-section .trust-badge img {
        height: 20px;
        width: 40px;
    }
  }
</style>