<!doctype html>
<html>
<head>
  <title>{{ page_title }}</title>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <meta name="description" content="{{ page_description | escape }}">
  <link rel="canonical" href="{{ canonical_url }}">
  <meta name="viewport" content="width=device-width,initial-scale=1,shrink-to-fit=no">
  {%- if settings.favicon != blank -%}
    <link rel="shortcut icon" href="{{ settings.favicon | img_url: '32x32' }}" type="image/png">
    {%- endif -%}
  
  {{ 'common.css' | asset_url | stylesheet_tag }}
  {{ 'styles.css' | asset_url | stylesheet_tag }}
   <!-- {{ 'bundle.css' | asset_url | stylesheet_tag }}
 {{ '00_normalize.scss' | asset_url | stylesheet_tag }}
  {{ '01_fonts.scss' | asset_url | stylesheet_tag }}
  {{ '02_grid.scss' | asset_url | stylesheet_tag }}
  {{ '03_swiper.min.css' | asset_url | stylesheet_tag }}
  {{ '05_jquery.fancybox.css' | asset_url | stylesheet_tag }}
  {{ '07_styles.scss' | asset_url | stylesheet_tag }}
  {{ '08_media.scss' | asset_url | stylesheet_tag }} -->
  {{ 'jquery-3.4.1.min.js' | asset_url | script_tag }}
  {{ 'libs.js' | asset_url | script_tag }}
  {{ 'common.js' | asset_url | script_tag }}
  <script src="https://cdn-widgetsrepository.yotpo.com/v1/loader/5AyB8SFKOjnnor-g4qIeMg" async></script>
{% if _page_path[1] == blank %}

    <link rel="canonical" href="{{canonical_url}}" />
    <link rel="alternate" href="https://curalife.com" hreflang="en-us" />
    <link rel="alternate" href="https://curalife.com" hreflang="x-default" />
    <link rel="alternate" href="https://global.curalife.com/it" hreflang="it" />
    <link rel="alternate" href="https://global.curalife.com" hreflang="en-gb" />

    {% elsif _page_path[1] == "it" %}

    <link rel="canonical" href="{{canonical_url}}" />
    <link rel="alternate" href="https://curalife.com{{ _page_path_normal | remove_first: '/it' }}" hreflang="en-us" />
    <link rel="alternate" href="https://curalife.com{{ _page_path_normal | remove_first: '/it' }}" hreflang="x-default" />
    <link rel="alternate" href="https://global.curalife.com{{ _page_path_normal }}" hreflang="it" />
    <link rel="alternate" href="https://global.curalife.com{{ _page_path_normal | remove_first: '/it' }}" hreflang="en-gb" />

    {% else %}

    <link rel="canonical" href="{{canonical_url}}" />
    <link rel="alternate" href="https://curalife.com{{ _page_path_normal }}" hreflang="en-us" />
    <link rel="alternate" href="https://curalife.com{{ _page_path_normal }}" hreflang="x-default" />
    <link rel="alternate" href="https://global.curalife.com/it{{ _page_path_normal }}" hreflang="it" />
    <link rel="alternate" href="https://global.curalife.com{{ _page_path_normal }}" hreflang="en-gb" />
  
  {% endif %}
{% if template contains 'customers' %}
    {{ "shopify_common.js" | shopify_asset_url | script_tag }}
    {{ "customer_area.js"  | shopify_asset_url | script_tag }}
{% endif %}
{{ content_for_header }} <!-- Header hook for plugins -->

  {% include 'head'%}

  {% render 'shogun-head' %}
</head>
<body>
{% include 'google-tag-manager'%}
{% include 'google-tag-manager-noscript'%}
{% include 'hot-jar'%}
  {% section 'header'%}

  <main role="main">
    {{ content_for_layout }}
    
     {% section 'footer'%}
  </main>
  <script>
  jQuery(document).on("click", ".remove-item", function () {
  
    var variantId =$(this).data("variant");
    var price =$(this).data("price");
    var title =$(this).data("title");
    var id =$(this).data("id");

  
    window.dataLayer  = window.dataLayer || [];
    window.dataLayer.push({
      'event': 'eec.removeFromCart',  // name of an event. In this case, always stays as eec.removeFromCart
      'ecommerce': {                  // ecommerce object. This is the essential part of tracking. New EE data must always be pushed to this object
        'currencyCode': 'USD',      // the currency which is currently used by a visitor
        'remove': {                 // name of an action. In this case, always stays as remove                           
          'products': [{              // list of products removed from a cart. can include multiple products.
            'name': title,      // name of a product that is currently viewed. Always include it.
            'id': id,           // id of a product. Always include it.
            'price': price.toString(),          // price of a product
            'category': 'Simple',      // category of a product
            'variant': variantId         // product variant.
          }]
        }
      },
      'userId': '{{ customer.id }}',
      'email': '{{ customer.email }}',
      'country': '{{customer.customer_address.country_code }}',
      'city': '{{customer.customer_address.city }}',
      'name': '{{ customer.first_name }}',
      'surname': '{{ customer.last_name }}',
      'phone_number': '{{customer.phone }}',
      'state': '{{customer.customer_address.province }}',       //state or province
      'zip': '{{customer.customer_address.zip }}',          //ZIP or Postal code
    });

  }); 
  </script> 
{% include 'salesforce-chat' %}
</body>
</html>
